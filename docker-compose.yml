# docker-compose.yml
version: '3.8'

services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - DATABASE_URL=postgres://labback:zPOMgNvInIVx8ImLMwmHrq5ngTPNOsIs@dpg-cmp4qvuct0pc73evuhbg-a/labback

volumes:
  postgres_data:
