# docker-compose.yml
version: '3.8'

services:
  app:
    build: .
    ports:
      - "8080:8080"
    depends_on:
      - db


  db:
    image: postgres:12
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: labBack
      POSTGRES_PASSWORD: somekindofpassword
      POSTGRES_DB: labBack
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
